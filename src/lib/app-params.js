// app-params.js
// Previously this file contained Base44-specific configuration logic. It
// is no longer needed; we simply export an empty object that can be extended
// by custom code if required.

export const appParams = {};
